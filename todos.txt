IMMEDIATE (validates algorithm before experiments):
  [x] Parse .bus benchmark files → bus_loader.py adapter
  [x] Run graph-free mode (Euclidean fallback) on all 10 instances
  [x] Compare objective values against Lewis paper published results
      STATUS: DONE — see benchmarks/compare_lewis.py
      Route count match: 4/10 (exact), 6/10 use 1 extra route.
      Journey time gap: +189% to +365% vs Lewis best.
      Root cause: our system visits ~20 stops/route (virtual stop per
      student); Lewis selects a small subset (~5-8 stops/route) and
      clusters students at them. Different problem formulations —
      we solve routing-with-insertion, they solve stop-selection+routing.
      CONCLUSION: direct objective comparison is not meaningful.
      Benchmarks validate feasibility (100% served) and scalability,
      not solution quality parity with Lewis.
  [x] Record runtime for Brisbane + Adelaide → scalability table
      STATUS: Done. Brisbane=499s/757students, Adelaide=151s/565students.

PRIMARY CONTRIBUTION (safety vs efficiency):
  [x] Build generate_dataset.py
        - gaussian_annulus distribution (peak_km, sigma_km, min_km, max_km)
        - highway node filter (no Ring Road snapping)
        - outputs valid {meta, data} JSON
        - CLI: --n_students --seed --output
  [x] Add "unconstrained" mode flag to run_algorithm.py
        - safe_to_cross = True for ALL road types
        - walk_limit = 400m flat, no age differentiation
  [x] Run Experiment 1: safety vs unconstrained (DONE)
        - n_students=40, 5 seeds, both modes, 20 ALNS iters
        - constrained: 96% served avg, 397 min avg time
        - unconstrained: 99% served avg, 254 min avg time
        - Saved: experiment1_results.csv
  [x] Run Experiment 2: scalability on Cairo graph (DONE)
        - n_students ∈ {20, 40, 60, 80, 100}, 20 ALNS iters
        - runtime: 7s → 14s → 23s → 34s → 14s
        - Saved: experiment2_results.csv

PAPER WRITING (parallel with above):
  [ ] Document safety constraint formulation
  [ ] Table: benchmark results vs published
  [ ] Table: safety vs unconstrained comparison
  [ ] Figure: scalability runtime plot
  [ ] Figure: route map example (real Cairo, best seed)

IF TIME PERMITS:
  [ ] Real Cairo school dataset (case study section)
  [ ] gmaps post-optimization display